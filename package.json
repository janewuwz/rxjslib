{
  "name": "rxjslib",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "observable": "tsc -p . --outDir dist && mocha dist/src/__test__/observable-test.js",
    "interval": "tsc -p . --outDir dist && mocha dist/src/__test__/interval-test.js",
    "combineLatest": "tsc -p . --outDir dist && mocha dist/src/__test__/combineLatest-test.js",
    "merge": "tsc -p . --outDir dist && mocha dist/src/__test__/merge-test.js",
    "subject": "tsc -p . --outDir dist && mocha dist/src/__test__/subject-test.js",
    "switchMap": "tsc -p . --outDir dist && mocha dist/src/__test__/switchMap-test.js",
    "filter": "tsc -p . --outDir dist && mocha dist/src/__test__/filter-test.js",
    "concat": "tsc -p . --outDir dist && mocha dist/src/__test__/concat-test.js",
    "zip": "tsc -p . --outDir dist && mocha dist/src/__test__/zip-test.js",
    "build": "tsc -p . --outDir dist",
    "dom": "tsc ./fromEvent-test.ts -m umd --outDir dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/janewuwz/rxjslib.git"
  },
  "keywords": [
    "rxjs"
  ],
  "author": "wz",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/janewuwz/rxjslib/issues"
  },
  "homepage": "https://github.com/janewuwz/rxjslib#readme",
  "devDependencies": {
    "@types/chai": "^4.1.3",
    "@types/mocha": "^5.2.0",
    "babel": "^6.23.0",
    "chai": "^4.1.2",
    "mocha": "^5.1.1",
    "ts-node": "^6.0.2",
    "typescript": "^2.8.3"
  }
}
